<UserControl x:Class="MediaDevicesDemo.View.DeviceExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:MediaDevicesDemo.View"
             xmlns:vm="clr-namespace:MediaDevicesDemo.ViewModel" 
             d:DataContext="{d:DesignInstance Type=vm:DeviceViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="7">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <TreeView Grid.Column="0" ItemsSource="{Binding Directories}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectedItemChanged">
                    <i:InvokeCommandAction Command="{Binding OnDirectorySelectedCommand}" CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=TreeView}}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </TreeView>
        <ListView Grid.Column="1" ItemsSource="{Binding Files}" SelectedItem="{Binding SelectedFile}"/>

    </Grid>
</UserControl>

<UserControl x:Class="MediaDevicesDemo.View.DeviceServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaDevicesDemo.View"
             xmlns:vm="clr-namespace:MediaDevicesDemo.ViewModel" 
             d:DataContext="{d:DesignInstance Type=vm:DeviceViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Content="Service Type"/>
        <ComboBox Grid.Column="1" Grid.Row="0" ItemsSource="{Binding ServiceTypes}" SelectedItem="{Binding SelectedServiceType}" Margin="3"/>

        <Label Grid.Column="0" Grid.Row="1" Content="Services"/>
        <ListView Grid.Column="1" Grid.Row="1" ItemsSource="{Binding Services}" SelectedItem="{Binding SelectedService}" Margin="3"/>
    </Grid>
</UserControl>

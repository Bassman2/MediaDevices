<UserControl x:Class="MediaDevicesDemo.View.DeviceCapabilitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaDevicesDemo.View"
             xmlns:vm="clr-namespace:MediaDevicesDemo.ViewModel" 
             d:DataContext="{d:DesignInstance Type=vm:DeviceViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Column="0" Content="Commands"/>
        <ListBox Grid.Column="0" Grid.Row="2" ItemsSource="{Binding Commands}" SelectedItem="{Binding SelectedCommand}" SelectedIndex="0" Margin="3"/>
        <ListBox Grid.Column="0" Grid.Row="3" ItemsSource="{Binding CommandOptions}" Margin="3"/>

        <Label Grid.Column="1" Content="Functional Objects"/>
        <ComboBox Grid.Column="1" Grid.Row="1" ItemsSource="{Binding FunctionalCategories}" SelectedItem="{Binding SelectedFunctionalCategory}" SelectedIndex="0" Margin="3"/>
        <ListBox Grid.Column="1" Grid.Row="2" ItemsSource="{Binding FunctionalObjects}" Margin="3"/>

        <Label Grid.Column="2" Content="Content Types"/>
        <ComboBox Grid.Column="2" Grid.Row="1" ItemsSource="{Binding FunctionalCategories}" SelectedItem="{Binding SelectedContentCategory}" SelectedIndex="0" Margin="3"/>
        <ListBox Grid.Column="2" Grid.Row="2" ItemsSource="{Binding ContentTypes}" SelectedItem="{Binding SelectedContentType}" Margin="3"/>
        <ListBox Grid.Column="2" Grid.Row="3" ItemsSource="{Binding Formats}" Margin="3"/>

        <Label Grid.Column="3" Content="Events"/>
        <ListBox Grid.Column="3" Grid.Row="2" ItemsSource="{Binding Events}" Margin="3"/>
    </Grid>
</UserControl>

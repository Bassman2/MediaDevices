<Window x:Class="MediaDevicesDemo.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaDevicesDemo.View"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Window.Resources>
        <Image x:Key="closeImage" Width="16" Height="16" Source="/Images/Close16.png" x:Shared="false"/>
        <Image x:Key="helpImage" Width="16" Height="16" Source="/Images/Help16.png" x:Shared="false"/>
        <Image x:Key="infoImage" Width="16" Height="16" Source="/Images/Info16.png" x:Shared="false"/>
    </Window.Resources>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <!--<MenuItem Header="_File">
                <MenuItem Header="Edit" Icon="{StaticResource editImage}" Command="{Binding EditCommand}" />
                <Separator/>
                <MenuItem Header="_Exit" Icon="{StaticResource closeImage}" Command="{Binding ExitCommand}" />
            </MenuItem>-->
            <!--<MenuItem Header="_Cache">
                <MenuItem Header="Clear and Update All" Icon="{StaticResource delImage}" Command="{Binding ClearCacheCommand}" ToolTip="Clear and Update All" />
                <MenuItem Header="Update All" Icon="{StaticResource scanImage}" Command="{Binding UpdateCacheCommand}" ToolTip="Update All"/>
                <MenuItem Header="Update Groups" Icon="{StaticResource scanImage}" Command="{Binding UpdateGroupCacheCommand}" ToolTip="Update Groups"/>
                <MenuItem Header="Update Users" Icon="{StaticResource scanImage}" Command="{Binding UpdateUserCacheCommand}" ItemsSource="{Binding CacheUpdates}" ToolTip="Update Users"/>
            </MenuItem>
            <MenuItem Header="_Work">
                <MenuItem Header="Check Groups" Icon="{StaticResource taskImage}" Command="{Binding CheckGroupsCommand}" />
                <MenuItem Header="Create Group Graph" Icon="{StaticResource graphImage}" Command="{Binding ExportGraphCommand}" />
            </MenuItem>
            <MenuItem Header="_Documentations" ItemsSource="{Binding Documentations}" >
                <MenuItem.ItemContainerStyle>
                    <Style TargetType="MenuItem" >
                        <Setter Property="Header" Value="{Binding Name}" />
                        <Setter Property="Icon" Value="{Binding Icon}" />
                        <Setter Property="Command" Value="{Binding OpenCommand}" />
                    </Style>
                </MenuItem.ItemContainerStyle>
            </MenuItem>-->
            <MenuItem Header="_Help">
                <MenuItem Header="About MediaDevices Demo" Icon="{StaticResource infoImage}" Command="{Binding AboutCommand}" />
            </MenuItem>
        </Menu>
        <ToolBarTray DockPanel.Dock="Top">
            <!--<ToolBar>
                <Button Command="{Binding CheckGroupsCommand}" Content="{StaticResource taskImage}" ToolTip="Check Groups"/>
                <Button Command="{Binding ExportGraphCommand}" Content="{StaticResource graphImage}" ToolTip="Create Graph"/>
                <Separator/>
            </ToolBar>
            <ToolBar ItemsSource="{Binding Documentations}">
                <ToolBar.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Icon}" ToolTip="{Binding Name}" Command="{Binding OpenCommand}" BorderBrush="{x:Null}" Background="{x:Null}" BorderThickness="0" Width="20" Height="20" />
                    </DataTemplate>
                </ToolBar.ItemTemplate>
            </ToolBar>-->
            <ToolBar>
                <Button Command="{Binding AboutCommand}" Content="{StaticResource infoImage}" ToolTip="About"/>
            </ToolBar>
        </ToolBarTray>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <!--<ColumnDefinition Width="Auto" />-->
                            <ColumnDefinition Width="32" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem Grid.Column="0">
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <StatusBarItem Grid.Column="1">
                <!--<Image Source="{Binding Caching, Converter={StaticResource BoolToImageBallConverter}}" ToolTip="{Binding Caching, Converter={StaticResource CachingToTooltipConverter}}" Width="20" Height="20"/>-->
            </StatusBarItem>
        </StatusBar>
        <TabControl>
            <TabItem Header="Properties">
                <local:DevicePropertiesView/>
            </TabItem>
            <TabItem Header="Capabilities">
                <local:DeviceCapabilityView/>
            </TabItem>
            
        </TabControl>

    </DockPanel>
</Window>
